# Lumare Landing

Simple Vite + React landing page for Lumare.

## Local development
- Install dependencies: `npm install`
- Run dev server: `npm run dev` (opens at http://localhost:3000)

## Deployment
- GitHub Pages is configured via `.github/workflows/deploy.yaml`.
- Vite base path is set to `/lumare-landing/` for the GitHub Pages site.
